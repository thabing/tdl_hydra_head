<div class="subnav">
<div class="span8 offset3">
   <ul class="nav nav-pills">
     <%= form_tag catalog_index_path, :class=>"form-inline", :method => :get do %>
    <i class="icon-search"></i>  <%= text_field_tag :q, params[:q], :class => "q input-medium" %>
         <% unless search_fields.empty? %>
             <%= select_tag(:search_field, options_for_select(search_fields, h(params[:search_field])), :title => "Targeted search options", :class=>"span2") %>
             <% end %>
    <button type="submit" class="btn">Search</button>&nbsp;
     <%= link_to 'Advanced Search', params.merge(:controller=>"advanced", :action=>"index") , :class=>"advanced_search"%>
     <% end %>
    </ul>
</div>
  </div>
<!--<div class="subnav">
<div class="span8 offset3">
   <ul class="nav nav-pills">   <form class="form-inline">
    <i class="icon-search"></i> <input type="text" class="input-medium">
<select class="span2" id="search_field" name="search_field" placeholder="Search" title="Targeted search options">
<option value="all_fields">Keyword</option>
<option value="title">Title</option>
<option value="author">Author</option>
<option value="subject">Subject</option></select>
    <button type="submit" class="btn">Search</button>&nbsp;
     <%= link_to 'Advanced Search', params.merge(:controller=>"advanced", :action=>"index") , :class=>"advanced_search"%>
    </form>
    </ul>
</div>
  </div>
-->
<!--
<div id="search" class="search">
  <%= form_tag catalog_index_path, :method => :get do %>
    <h2 class="search"><%= label_tag(:q, "Search ") %></h2>
    <%= text_field_tag :q, params[:q], :class => "q" %>
    <% unless search_fields.empty? %>
      <%= label_tag(:search_field, " in ") %>
      <%= select_tag(:search_field, options_for_select(search_fields, h(params[:search_field])), :title => "Targeted search options", :class=>"search_field") %>
    <% end %>
    <%= search_as_hidden_fields(:omit_keys => [:q, :search_field, :qt, :page]).html_safe %>

    <%= submit_tag 'search', :class=>'submit' %>
  <% end %>
  <%= link_to 'Advanced Search', params.merge(:controller=>"advanced", :action=>"index") , :class=>"advanced_search"%>


  <hr/>
</div>
-->