<% unless metadata[:subjects].empty? %>
    <dd>Subject</dd>
    <% metadata[:subjects].each do |subject| %>
        <dt><%= link_to((raw subject.text), "/catalog?f[subject_facet][]="+ (raw subject.text)) %></dt>
    <% end %>
<% end %>