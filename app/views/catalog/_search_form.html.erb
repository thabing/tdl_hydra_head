<div id="search" class="search">
  <%= form_tag catalog_index_path, :method => :get do %>
    <h2 class="search"><%= label_tag(:q, "Search ") %></h2>
    <%= text_field_tag :q, params[:q], :class => "q" %>
    <% unless search_fields.empty? %>
      <%= label_tag(:search_field, " in ") %>
      <%= select_tag(:search_field, options_for_select(search_fields, h(params[:search_field])), :title => "Targeted search options", :class=>"search_field") %>
    <% end %>
    <%= search_as_hidden_fields(:omit_keys => [:q, :search_field, :qt, :page]).html_safe %>

    <%= submit_tag 'search', :class=>'submit' %>
  <% end %>
  <%= link_to 'Advanced Search', params.merge(:controller=>"advanced", :action=>"index") , :class=>"advanced_search"%>


  <hr/>
</div>
<!--
<div id="search" class="search">
  <%= form_tag catalog_index_path, :method => :get do %>
    <h2 class="search"><%= label_tag(:q, "Search ") %></h2>
    <%= text_field_tag :q, params[:q], :class => "q"  %>

    <%= search_as_hidden_fields(:omit_keys => [:q, :search_field, :qt]) %>

    <%= image_submit_tag('tdl/search_button.png', :plugin=>"hydra-head", :id => "submit", :alt=>"Search") %>
  <% end %>

</div>

<form action="/" method="get" title="Search form">
      <span id="search_box_and_link">
        <input class="search" id="q" name="q" type="text" />
    	</span>

      <noscript>
        <label for="search_field"> in </label>
        <select id="search_field" name="search_field" title="Targeted search options"><option value="search">Everything</option>
<option value="search_author">Author</option>
<option value="search_title">Title</option>
<option value="subject_terms">Subject terms</option>
<option value="call_number">Call number</option></select>
      </noscript>
    	<div id="split_button" style="display:none;">
    	  <div class="button_set">
      		<button id="run">
      			  Search
      		</button>
      		<button id="select">select</button>
    		</div>

    		<div class="drop_down" style="display:none;">
    			<ul>
    				  <li class="search"><a href="">Everything</a></li>
    				  <li class="search_author"><a href="">Author</a></li>
    				  <li class="search_title"><a href="">Title</a></li>
    				  <li class="subject_terms"><a href="">Subject terms</a></li>
    				  <li class="call_number"><a href="">Call number</a></li>

    		  </ul>
    	  </div>
    	</div>
      <input id="hidden_search_field" name="hidden_search_field" type="hidden" value="search" />


      <noscript>
        <input class="submit" name="commit" type="submit" value="search" />
      </noscript>
    </form>
    -->
